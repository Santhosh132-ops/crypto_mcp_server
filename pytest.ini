[pytest]

asyncio_mode = auto


# This tells the pytest-asyncio plugin to auto-detect and run
# all tests defined with 'async def' without needing the
# @pytest.mark.asyncio decorator on every test function.

